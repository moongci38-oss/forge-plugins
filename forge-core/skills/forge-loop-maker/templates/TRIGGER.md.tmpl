<!-- root-cause: forge-loop-maker scaffold 템플릿 — TRIGGER.md 생성용 -->
# {{LOOP_NAME}} — Trigger Definition

---

## Verifiable goal

루프 종료 predicate:

> **{{GOAL}}**

---

## State file

```
{{STATE_PATH}}
```

매 실행 시작 시 읽고 종료 전 씁니다.
파일 없으면 첫 실행으로 간주 → ledger 초기화 후 시작.

---

## Trigger: {{TRIGGER}}

### Claude Code

```
/goal "{{GOAL}}"
```

### 수동 실행 (모든 플랫폼)

```
{{STATE_PATH}} 와 SKILL.md를 읽고 {{LOOP_NAME}} 루프를 실행하세요.
```

---

## 실행 전 체크리스트

1. `{{STATE_PATH}}` 존재 + 초기화 확인
2. HUMAN-GATES.md G1 gate 완료
3. verifier exit 0 확인
4. Budget: max-iter={{MAX_ITER}}, call-budget={{CALL_BUDGET}}, wall-clock={{WALL_CLOCK}}
