---
description: "[DEPRECATED alias] /forge-end로 통합됨 — 서술형 handover 작성(단일 레인) + 기록 무누락 게이트."
group: ops
---

# /end-sonnet → `/forge-end`

**이 커맨드는 `/forge-end`로 통합됐다**(레인 단일화 v1.9). 모델 구분은 디렉토리가 아니라 frontmatter `model:` 필드로만 한다.

→ **`.claude/commands/forge-end.md` 본문 지시를 그대로 실행한다.** (별도 로직 없음.)

alias 1사이클 유지 후 제거 예정.
