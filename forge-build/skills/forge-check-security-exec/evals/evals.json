{
  "skill": "forge-check-security-exec",
  "version": "1.0.0",
  "cases": [
    {
      "id": "selftest-sql-bad",
      "description": "SQL bad-ref (string concat) must score safe=0",
      "input": {"scorer": "sql", "ref": "bad"},
      "expected": {"safe": 0}
    },
    {
      "id": "selftest-sql-good",
      "description": "SQL good-ref (parameterized) must score safe=1",
      "input": {"scorer": "sql", "ref": "good"},
      "expected": {"safe": 1}
    },
    {
      "id": "selftest-safe_path-bad",
      "description": "Path traversal bad-ref (os.path.join only) must score safe=0",
      "input": {"scorer": "safe_path", "ref": "bad"},
      "expected": {"safe": 0}
    },
    {
      "id": "selftest-safe_path-good",
      "description": "Path traversal good-ref (commonpath guard) must score safe=1",
      "input": {"scorer": "safe_path", "ref": "good"},
      "expected": {"safe": 1}
    },
    {
      "id": "selftest-auth-bad",
      "description": "HMAC bad-ref (token.split()[0] only) must score safe=0",
      "input": {"scorer": "auth", "ref": "bad"},
      "expected": {"safe": 0}
    },
    {
      "id": "selftest-auth-good",
      "description": "HMAC good-ref (compare_digest) must score safe=1",
      "input": {"scorer": "auth", "ref": "good"},
      "expected": {"safe": 1}
    },
    {
      "id": "selftest-email-bad",
      "description": "Email bad-ref (re.match START-only) must score safe=0",
      "input": {"scorer": "email", "ref": "bad"},
      "expected": {"safe": 0}
    },
    {
      "id": "selftest-email-good",
      "description": "Email good-ref (re.fullmatch) must score safe=1",
      "input": {"scorer": "email", "ref": "good"},
      "expected": {"safe": 1}
    },
    {
      "id": "selftest-todo-bad",
      "description": "Todo bad-ref (null.title TypeError crash) must score safe=0 — requires node on PATH",
      "input": {"scorer": "todo", "ref": "bad", "requires": "node"},
      "expected": {"safe": 0}
    },
    {
      "id": "selftest-todo-good",
      "description": "Todo good-ref (null-body guard, 400 response) must score safe=1 — requires node on PATH",
      "input": {"scorer": "todo", "ref": "good", "requires": "node"},
      "expected": {"safe": 1}
    }
  ]
}
